#include <vector>
#include <string>

/*
This functions should take a string as input, and output a vector with all the words, and also execute preprocessor commands.
*/
std::vector<std::string> preprocess(std::string);