#include "cli.h"

namespace cli
{
    void parse_Command(std::vector<std::string> args)
    {
        int pos = 0;
        for(std::string word : args)
        {       
            pos++;
        }
    }
    void run_Command()
    {
        
    }
}