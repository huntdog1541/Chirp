#include <iostream>
#include <string>
#include <vector>

namespace cli
{
    /*
    Parses the command
    */
    void parse_Command(std::vector<std::string>);
    /*
    Runs the command
    */
    void run_Command(std::string, std::string);
}